{
  "category": "Vector-Scalar Instructions",
  "constraints": "!!! warning \"Constraints\"\n    - Bitwise forms are defined for integer element types only.\n    - `%input` and `%result` MUST have the same vector width `N` and element type `T`.\n    - The mask width MUST match `N`.",
  "cpp_intrinsic": "",
  "examples": [
    "for (int i = 0; i < N; i++)\n    if (mask[i])\n        dst[i] = src[i] & scalar;"
  ],
  "illegal_cases": "!!! danger \"Exceptions\"\n    - The verifier rejects illegal operand shapes, unsupported element types, and attribute combinations that are not valid for the selected instruction set or target profile.\n    - Any additional illegality stated in the constraints section is also part of the contract.",
  "instruction_set_path": [
    "9. Vector Instruction Reference",
    "Vector-Scalar Instructions"
  ],
  "mnemonic": "VANDS",
  "name": "pto.vands",
  "operands": [
    "input",
    "scalar",
    "mask"
  ],
  "outputs": [],
  "sources": [
    "docs/isa/vector/ops/vec-scalar-ops/vands.md"
  ],
  "summary": "Lane-wise bitwise AND of a vector register and a broadcast scalar.",
  "syntax": "vands %dst, %src, %scalar, %mask : !pto.vreg<NxT>, T",
  "target_profile_restrictions": "??? info \"Target-Profile Restrictions\"\n    - Integer element types only.\n    - A5 is the most detailed concrete profile in the current manual; CPU simulation and A2/A3-class targets may support narrower subsets or emulate the behavior while preserving the visible PTO contract."
}
