{
  "category": "Vector-Scalar Instructions",
  "constraints": "!!! warning \"Constraints\"\n    - Carry-chain forms are defined for integer element types.\n    - `%lhs`, `%rhs`, and `%result` MUST have the same vector width `N` and element type `T`.\n    - `%carry_in`, `%carry`, and `%mask` MUST all have width `N`.",
  "cpp_intrinsic": "",
  "examples": [
    "for (int i = 0; i < N; i++) {\n    if (!mask[i]) continue;\n    uint64_t sum = (uint64_t)lhs[i] + rhs[i] + carry_in[i];\n    result[i] = (T)sum;\n    carry[i] = (sum >> BITWIDTH_T) & 0x1;\n}"
  ],
  "illegal_cases": "!!! danger \"Exceptions\"\n    - The verifier rejects illegal operand shapes, unsupported element types, and attribute combinations that are not valid for the selected instruction set or target profile.\n    - Any additional illegality stated in the constraints section is also part of the contract.",
  "instruction_set_path": [
    "9. Vector Instruction Reference",
    "Vector-Scalar Instructions"
  ],
  "mnemonic": "VADDCS",
  "name": "pto.vaddcs",
  "operands": [
    "lhs",
    "rhs",
    "carry_in",
    "mask"
  ],
  "outputs": [
    "carry"
  ],
  "sources": [
    "docs/isa/vector/ops/vec-scalar-ops/vaddcs.md"
  ],
  "summary": "Lane-wise add with explicit carry-in and carry-out masks.",
  "syntax": "vaddcs %dst, %carry_out, %lhs, %rhs, %carry_in, %mask : !pto.vreg<NxT>, !pto.mask<G>",
  "target_profile_restrictions": "??? info \"Target-Profile Restrictions\"\n    - Treat this form as an unsigned integer carry-chain unless a target profile documents a wider legal domain.\n    - A5 is the most detailed concrete profile in the current manual; CPU simulation and A2/A3-class targets may support narrower subsets or emulate the behavior while preserving the visible PTO contract."
}
