{
  "category": "Elementwise Tile Tile",
  "constraints": "!!! warning \"Constraints\"\n    - The op iterates over `dst.GetValidRow()` / `dst.GetValidCol()`.\n\n    - For A3, 2 source Tile, destination Tile, temporary space must in different memory range without overlapping.\n\n    - For A3, `tmp.GetValidRow()` must be greater than or equal to `dst.GetValidCol() + 1`.",
  "cpp_intrinsic": "template <typename TileDataDst, typename TileDataSrc0, typename TileDataSrc1, typename TileDataTmp,\n          typename... WaitEvents>\nPTO_INST RecordEvent TPRELU(TileDataDst &dst, TileDataSrc0 &src0, TileDataSrc1 &src1, TileDataTmp &tmp, WaitEvents &... events);",
  "examples": [
    "#include <pto/pto-inst.hpp>\n\nusing namespace pto;\n\nvoid example() {\n  using TileT = Tile<TileType::Vec, float, 16, 16>;\n  TileT x, slope, out, tmp;\n  TPRELU(out, x, slope, tmp);\n}"
  ],
  "illegal_cases": "!!! danger \"Exceptions\"\n    - Illegal operand tuples, unsupported types, invalid layout combinations, or unsupported target-profile modes are rejected by the verifier or by the selected backend instruction set.\n    - Programs must not rely on behavior outside the documented legal domain of this operation, even if one backend currently accepts it.",
  "instruction_set_path": [
    "8. Tile Instruction Reference",
    "Elementwise Tile Tile"
  ],
  "mnemonic": "TPRELU",
  "name": "pto.tprelu",
  "operands": [
    "src0",
    "src1",
    "tmp"
  ],
  "outputs": [
    "dst"
  ],
  "sources": [
    "docs/isa/tile/ops/elementwise-tile-tile/tprelu.md"
  ],
  "summary": "Elementwise PReLU (parametric ReLU) with a per-element slope tile.",
  "syntax": "%dst = tprelu %src0, %src1 : !pto.tile<...>",
  "target_profile_restrictions": "??? info \"Target-Profile Restrictions\"\n    - Temporary space is required by A3 for calculation, while not used by A5."
}
