{
  "category": "Irregular And Complex",
  "constraints": "!!! warning \"Constraints\"\n    - `isUpperOrLower` must be `0` (lower) or `1` (upper).\n\n    - Destination tile must be row-major on some targets (see `include/pto/npu/*/TTri.hpp`).",
  "cpp_intrinsic": "template <typename TileData, int isUpperOrLower, typename... WaitEvents>\nPTO_INST RecordEvent TTRI(TileData &dst, int diagonal, WaitEvents &... events);",
  "examples": [
    "# Auto mode: compiler/runtime-managed placement and scheduling.\n%dst = pto.ttri %src0, %src1 : (!pto.tile<...>, !pto.tile<...>) -> !pto.tile<...>"
  ],
  "illegal_cases": "!!! danger \"Exceptions\"\n    - Illegal operand tuples, unsupported types, invalid layout combinations, or unsupported target-profile modes are rejected by the verifier or by the selected backend instruction set.\n    - Programs must not rely on behavior outside the documented legal domain of this operation, even if one backend currently accepts it.",
  "instruction_set_path": [
    "8. Tile Instruction Reference",
    "Irregular And Complex"
  ],
  "mnemonic": "TTRI",
  "name": "pto.ttri",
  "operands": [],
  "outputs": [],
  "sources": [
    "docs/isa/tile/ops/irregular-and-complex/ttri.md"
  ],
  "summary": "Generate a triangular (lower/upper) mask tile.",
  "syntax": "%dst = pto.ttri %src0, %src1 : (!pto.tile<...>, !pto.tile<...>) -> !pto.tile<...>",
  "target_profile_restrictions": "??? info \"Target-Profile Restrictions\"\n    - `pto.ttri` preserves PTO-visible semantics across CPU simulation, A2/A3-class targets, and A5-class targets, but concrete support subsets may differ by profile.\n\n    - Portable code must rely only on the documented type, layout, shape, and mode combinations that the selected target profile guarantees."
}
