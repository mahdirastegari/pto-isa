{
  "category": "Predicate Generation And Algebra",
  "constraints": "!!! warning \"Constraints\"\n    - **Operand widths**: All predicate operands MUST have the same width. Mixing predicates of different widths without explicit pack/unpack is **illegal**.\n    - **No implicit masking**: The `mask` operand is for scalar and control instructions use; it does not affect the boolean AND operation itself.",
  "cpp_intrinsic": "vector_bool dst;\nvector_bool src0;\nvector_bool src1;\nvector_bool mask;\npand(dst, src0, src1, mask);",
  "examples": [
    "#include <pto/pto-inst.hpp>\nusing namespace pto;\n\nvoid combine_masks(RegBuf<predicate_t>& dst,\n                   const RegBuf<predicate_t>& cmp_mask,\n                   const RegBuf<predicate_t>& tail_mask) {\n    PAND(dst, cmp_mask, tail_mask, cmp_mask);\n}"
  ],
  "illegal_cases": "!!! danger \"Exceptions\"\n    - Illegal if predicate operand widths are not consistent.",
  "instruction_set_path": [
    "10. Scalar And Control Reference",
    "Predicate Generation And Algebra"
  ],
  "mnemonic": "PAND",
  "name": "pto.pand",
  "operands": [
    "src0",
    "src1",
    "mask"
  ],
  "outputs": [
    "dst"
  ],
  "sources": [
    "docs/isa/scalar/ops/predicate-generation-and-algebra/pand.md"
  ],
  "summary": "Bitwise AND of two predicates.",
  "syntax": "%dst = pto.pand %src0, %src1, %mask : !pto.mask<G>, !pto.mask<G>, !pto.mask<G> -> !pto.mask<G>",
  "target_profile_restrictions": "??? info \"Target-Profile Restrictions\"\n    | Aspect | CPU Sim | A2/A3 | A5 |\n    |--------|:-------:|:------:|:--:|\n    | Bitwise AND | Simulated | Supported | Supported |"
}
