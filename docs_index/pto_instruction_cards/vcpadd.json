{
  "category": "Reduction Instructions",
  "constraints": "!!! warning \"Constraints\"\n    - The current manual documents floating-point forms.\n    - The mask width MUST match `N`.\n    - Inactive lanes contribute zero to later active-lane prefixes.",
  "cpp_intrinsic": "",
  "examples": [
    "T running = 0;\nfor (int i = 0; i < N; i++) {\n    if (mask[i]) running += src[i];\n    dst[i] = running;\n}"
  ],
  "illegal_cases": "!!! danger \"Exceptions\"\n    - The verifier rejects illegal operand shapes, unsupported element types, and attribute combinations that are not valid for the selected instruction set or target profile.\n    - Any additional illegality stated in the constraints section is also part of the contract.",
  "instruction_set_path": [
    "9. Vector Instruction Reference",
    "Reduction Instructions"
  ],
  "mnemonic": "VCPADD",
  "name": "pto.vcpadd",
  "operands": [
    "input",
    "mask"
  ],
  "outputs": [],
  "sources": [
    "docs/isa/vector/ops/reduction-ops/vcpadd.md"
  ],
  "summary": "Inclusive prefix-sum (scan) over the active lanes.",
  "syntax": "vcpadd %dst, %src, %mask : !pto.vreg<NxT>",
  "target_profile_restrictions": "??? info \"Target-Profile Restrictions\"\n    - Documented A5 coverage: `f16`, `f32`.\n    - A5 is the most detailed concrete profile in the current manual; CPU simulation and A2/A3-class targets may support narrower subsets or emulate the behavior while preserving the visible PTO contract."
}
